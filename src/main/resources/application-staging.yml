spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vitalittedb_staging?allowPublicKeyRetrieval=true&useSSL=false

vitalitte-project:
  app:
    controller:
      cross-origin: "*"
    jwtSecret: staging-jwt-secret
