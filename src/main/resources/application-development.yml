spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vitalittedb_dev?allowPublicKeyRetrieval=true&useSSL=false

vitalitte-project:
  app:
    controller:
      cross-origin: "*"
    jwtSecret: dev-jwt-secret
